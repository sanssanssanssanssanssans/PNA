user: {
  name: "Alice",
  age: 17,
}

i: 0

log "Hello, " + user.name

cond (user.age >= 18) -> {
  log "adult"
} else -> {
  log "minor"
}
end

loop (i < 3) -> {
  log "i=" + i
  i: i + 1
}
end

input "Your favorite fruit?" -> user.fav
log upper user.fav

log randint 1 5
log slice "abcdef" 1 4